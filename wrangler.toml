[env.production.vars]
FRAMEWORK = "vue"
not_found_handling = "single_page_application"

[
triggers
]
routes = [
  { pattern = "tinyflake.cn", zone_name = "tinyflake.cn", custom_domain = true, override_existing_dns_record = true }
]
